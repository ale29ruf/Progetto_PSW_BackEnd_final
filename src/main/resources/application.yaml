spring:
  application:
    name: Temp-Store


#Configurazione dell'unità di persistenza
  datasource:
    url: jdbc:postgresql://localhost:5432/database_psw
    password: Computer@2002
    driver-class-name: org.postgresql.Driver
    username: postgres
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect #usato per convertire le query da JPQL in quelle specifiche di postgres
    hibernate:
      ddl-auto: update



server:
  #ssl:
  #key-store-password: password_certificate
  #key-store-type: PKCS12
  #key-store: /home/debian/ssl-cert/certificate.p12

  #key-store-password: stefano
  #key-store-type: PKCS12
  #enabled: true
  #key-store: C:\Users\Stefano\Desktop\sslcert1\certificate.p12
  #port: 9090
  compression:
    enabled: 'true'
  port: '8080'

  #security:
  #require-ssl: 'true'


